<% provide(:title, "New Post") %>
<center><h1>New Post</h1></center>

<%= render partial: 'posts/form', locals: { button_label: "Add Post", cancel_path: post_path(@post) } %>

<%= link_to 'Back', posts_path, {"data-no-turbolink" => true} %>
